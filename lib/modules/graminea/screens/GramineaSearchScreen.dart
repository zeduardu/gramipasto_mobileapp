import 'package:flutter/material.dart';

class GramineaSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("GramineaSearchScreen"),
      ),
      body: Center(
        child: ElevatedButton(
          onPressed: () {},
          child: Text('Voltar'),
        ),
      ),
    );
  }
}
